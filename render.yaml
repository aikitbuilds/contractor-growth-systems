services:
  - type: web
    name: contractor-growth-systems
    env: docker
    dockerfilePath: ./Dockerfile.production
    dockerContext: .
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production 