services:
  - type: web
    name: contractor-growth-systems
    env: docker
    dockerfilePath: Dockerfile.production
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000 